package com.example.demo;

import java.math.BigDecimal;

import reactor.core.publisher.Mono;

import org.springframework.security.access.annotation.Secured;
import org.springframework.security.access.prepost.PreAuthorize;
import org.springframework.stereotype.Component;

@Component
public class SalaryService {


	//@PreAuthorize("hasRole('ADMIN')")
	public Mono<String> getSalaryForEmployee(Employee employee) {
		return Mono.just("42");
	}

	//@Secured({ "ROLE_HR" })
	public Mono<String> updateSalary(String employeeId, String newSalary) {
	Employee emp =new Employee(employeeId,newSalary);
	return getSalaryForEmployee(emp);
	}

}
